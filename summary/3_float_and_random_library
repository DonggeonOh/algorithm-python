소수점 관련 라이브러리
from math import * # math 라이브러리 안에 있는 모든 함수 불러오기

floor(3.14) # 내림
ceil(3.14) # 올림
sprt(3.14) # 제곱근 - 16의 제곱근 4



---
랜덤 관련 라이브러리
from random import *

random() - 0.0 부터 1.0 사이의 소수점 값 반환
random() * 10 - 0.0 부터 10.0 사이의 소수점 값 반환
randrange(int, int) - A 이상 B 미만의 랜덤한 정수 값 반환
randint(int, int) - A 이상 B 이하의 랜덤한 정수 값 반환